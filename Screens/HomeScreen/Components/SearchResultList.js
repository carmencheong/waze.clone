import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

/*
If keywoard is empty this component will show three options:
- gas stations on click show gas stations.
- Favorite, on click show all favorite spots.
- search contacts on click show all contact locaitons.
if keyword is not empty:
- the component will search result and display them as clickable events.
*/
const SearchResultList = ({DATA, keyword}) => {
  return (
    <View>
      <Text></Text>
    </View>
  )
}

export default SearchResultList

const styles = StyleSheet.create({})
